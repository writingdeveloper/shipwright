/**
 * @repo/{{ dashCase name }}
 *
 * Placeholder entry point. Replace this with the package's real public API and
 * keep it the single export surface (`exports["."]` in package.json points here).
 *
 * Per the repo discipline (CONTRIBUTING.md): build the feature in a consumer
 * first and extract it here once the shape has proven out — don't grow an empty
 * abstraction.
 */
export const {{ camelCase name }}PackageName = "@repo/{{ dashCase name }}" as const;
