---
"create-shipwright": minor
---

Initial release of the `create-shipwright` scaffolding CLI. Run
`npx create-shipwright <dir>` to download the shipwright starter into a new
project, with `--dry-run`, `--template`, `--ref`, `--pm`, `--no-git`, and
`--no-install` flags.
