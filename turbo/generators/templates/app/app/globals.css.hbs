@import "@repo/ui/globals.css";

/* Tailwind v4 scans this app's source automatically; also scan the shared
   @repo/ui package source so classes used inside its components aren't purged. */
@source "../../../packages/ui/src/**/*.{ts,tsx}";
